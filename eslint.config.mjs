// @ts-check
import withNuxt from './.nuxt/eslint.config.mjs'

export default withNuxt(
  // Aquí puedes agregar reglas propias más adelante.
  // Por ahora usamos la configuración recomendada de Nuxt tal cual.
)
